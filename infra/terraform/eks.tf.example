# EKS cluster for Millo (example — uncomment and configure)
# Requires: terraform-aws-modules/eks/aws

# module "eks" {
#   source  = "terraform-aws-modules/eks/aws"
#   version = "~> 19.0"
#
#   cluster_name    = "millo-${var.environment}"
#   cluster_version = "1.28"
#
#   vpc_id     = module.vpc.vpc_id
#   subnet_ids = module.vpc.private_subnets
#
#   eks_managed_node_groups = {
#     default = {
#       min_size     = 3
#       max_size     = 20
#       desired_size = 3
#       instance_types = ["t3.medium"]
#     }
#   }
# }
