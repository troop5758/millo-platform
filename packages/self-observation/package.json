{
  "name": "@millo/self-observation",
  "version": "3.0.0",
  "private": true,
  "description": "Self-observation engine — drift, upgrade advisor, health, security. Read-only; no auto-changes.",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test src/*.test.js"
  },
  "engines": { "node": ">=18.0.0" },
  "optionalDependencies": {
    "bullmq": "^5.0.0"
  }
}
