/**
 * Global Design System (canonical tokens).
 *
 * Note: the current UI uses CSS variables defined in `src/index.css`.
 * This file provides a single source of truth for theme tokens so other
 * parts of the app can reference them without duplicating values.
 */
export const theme = {
  colors: {
    primary: '#ff5500', // TikTok style
    bg: '#000000',
    card: '#121212',
    text: '#ffffff',
    muted: '#888888',
  },
  radius: '16px',
};

