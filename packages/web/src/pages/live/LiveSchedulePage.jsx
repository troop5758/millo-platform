import React from 'react';
import { ScheduleStreamPage } from '../ScheduleStreamPage';

/**
 * LiveSchedulePage
 * Route: /live/schedule
 * Uses the existing scheduling UI (/schedule-stream).
 */
export function LiveSchedulePage() {
  return <ScheduleStreamPage />;
}

