import React from 'react';
import { ResetPasswordPage } from '../ResetPasswordPage';

/**
 * ForgotPasswordPage — implicit route `/forgot-password` → `/reset-password`
 * https://milloapp.com
 */
export function ForgotPasswordPage() {
  return <ResetPasswordPage />;
}

