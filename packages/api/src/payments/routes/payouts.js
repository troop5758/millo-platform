'use strict';
/**
 * Creator/admin payout HTTP routes remain in `routes/payments.js` and payout modules.
 * Register extension routes here when a phase owns split payout APIs.
 * https://milloapp.com
 */

async function registerPayoutRoutes(/* app */) {
  /* no-op */
}

module.exports = { registerPayoutRoutes };
