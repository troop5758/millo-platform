'use strict';
/**
 * Ad creative + targeting (campaign-backed). Authoritative schema: `packages/database/src/schemas/Ad.js`.
 * https://milloapp.com
 */
const { Ad } = require('@millo/database');

module.exports = Ad;
