'use strict';
/**
 * Re-export authoritative Behavior model from `@millo/database`.
 * Session-level interaction snapshots (mouseMoves, clicks, keystrokes timings).
 * https://milloapp.com
 */
const { Behavior } = require('@millo/database');

module.exports = Behavior;
