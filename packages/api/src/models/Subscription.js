'use strict';
/**
 * Viewer/creator subscription (platform or creator channel).
 * Authoritative schema: `packages/database/src/schemas/Subscription.js`.
 * https://milloapp.com
 */
const { Subscription } = require('@millo/database');

module.exports = Subscription;
