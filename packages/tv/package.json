{
  "name": "@millo/tv",
  "version": "3.0.0",
  "private": true,
  "description": "Smart TV clients — Apple TV / Android TV, read-only, device pairing.",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test src/*.test.js"
  },
  "engines": { "node": ">=18.0.0" },
  "dependencies": {
    "@millo/database": "3.0.0"
  }
}
